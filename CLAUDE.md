# このリポジトリでの作業ルール

Zenn(https://zenn.dev)に投稿する記事を管理するリポジトリです。

## ブランチ運用

- `main`: 公開用の最終ブランチ
- `dev`: 作業用ブランチ。記事の追加・修正はまずここに積む

Claude(Cowork/Claude Code)がこのリポジトリで作業する場合は、セッションごとに割り当てられる使い捨てのブランチ(`claude/xxxx-xxxx-xxx` 等)ではなく、**`dev` ブランチに直接コミット・プッシュする**こと。

記事がある程度まとまったら `dev` → `main` のプルリクエストを作成する。ただし、**マージするかどうか・いつマージするかはユーザー(hkawase)が判断する**ため、Claude側でPRを自動マージしないこと。

## 記事の下書きについて

- 新規記事は `published: false` で作成する(ユーザーが内容を確認・編集してから公開設定に変更する運用)
- 週次のセキュリティインシデントまとめ記事は、対象週(月〜日)ごとに1本作成する。同じ週を対象とした記事が既に `dev` または `main` に存在する場合は、重複して作成しない
